---
title: Calendar
---

import DocCalendar from "@site/src/pages/web-elements/calendar/doc-calendar"

###### 注意：在普通的 `React` + `bootstrap` 项目中，样式不会发生异常，而在 `docusaurus` 项目中，`样式`会`出现异常`，需要手动去设置样式

<DocCalendar />